import 'package:flutter/material.dart';

class AddSponsors extends StatelessWidget {
  const AddSponsors({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
